*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# AI agent outputs — generados automáticamente, no commitear
ai-output/

# Build outputs
app/build/
build/

# Claude Code — settings locales (pueden tener paths absolutos)
.claude/settings.local.json

# Secrets locales — nunca commitear
.env
scripts/.env

# Java heap dumps
*.hprof

# Node deps (scripts + MCP tools) — regenerables con npm install
node_modules/
**/node_modules/

# AAR local del SDK (build normal usa Maven; el .aar es para builds locales)
app/libs/

# Artefactos de herramientas exploratorias y capturas sueltas
midscene_run/
.claude/scheduled_tasks.lock
/screenshot_*.png
/ss_*.png

# IDE local
.vscode/
.idea/deploymentTargetSelector.xml
.idea/misc.xml

# Estado del watcher del SDK (última versión probada, por-rama) — runtime, no versionar
.last-tested-sdk*
